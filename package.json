{
  "name": "kode",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf lib && rm -rf node_modules && yarn unlink",
    "setup": "yarn install && yarn run build && yarn unlink; yarn link",
    "build": "tsc",
    "watch": "tsc -w",
    "test": "ava --watch --verbose",
    "test-serial": "ava --watch --verbose --serial"
  },
  "author": "KJK",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "ava": "^3.5.1"
  }
}